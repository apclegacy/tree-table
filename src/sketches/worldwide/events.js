let events = {
    melting: {
        text: "Ice Melting",
        lat: -42.248229,
        long: 61.864973
    }
}

function drawEvents() {
    /*Object.keys(events).forEach(key => {
        let pos = toCartesian(events[key].lat, events[key].long, r);
        let event = screenPosition(pos.x, pos.y, pos.z);
        fill(200, 200, 200);
        circle(event.x + windowWidth / 2, event.y + windowHeight / 2, 250);
    });*/
}
